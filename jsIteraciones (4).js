function mostrar()
{
	var numero = prompt("Ingrese un número entre 0 y 10."); 

	while(numero < o || numero > 9 || isNaN(numero)){

		numero = prompt("Ingrese un número valido.");
		numero = parseInt(numero);

}
}//FIN DE LA FUNCIÓN